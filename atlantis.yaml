version: 3
projects:
- dir: production
  workspace: production
  apply_requirements: [approved, mergeable] # conftest
  # apply_requirements: [approved] # command requirements
- dir: staging
  workspace: staging
  # apply_requirements: [approved] # command requirements